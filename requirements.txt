gunicorn app:app --bind 0.0.0.0:$PORT
```

**Тип экземпляра:**
- Выберите **Free** (бесплатный)

---

##⚠️ **ВАЖНО! Перед деплоем:**

### 1. **Создайте файл `requirements.txt`** в корне проекта:

Создайте файл и добавьте в него:
```
Flask==3.0.0
psycopg2-binary==2.9.9
python-dotenv==1.0.0
pandas==2.1.4
gunicorn==21.2.0